javascript:(function(){try{/* Show UI if hidden */if (window.$){$("#SpaceForAnswer").show();$("#SystemMessage").show();}/* Ensure answers are computed at least once */if (typeof SubmitForm === "function"){SubmitForm();}/* Round helper */function roundOneDecimal(x){return Math.round(x * 10) / 10;}/* Find all AnswerN variables dynamically */var answers = [];for (var i = 1; i <= 10; i++){if (typeof window["Answer" + i] !== "undefined"){answers.push(window["Answer" + i]);}}/* Fill corresponding inputs A1, A2, A3, ... */for (var j = 0; j < answers.length; j++){var inputId = "#A" + (j + 1);if (window.$ && $(inputId).length){$(inputId).val(roundOneDecimal(answers[j]));}}/* Final submit to render correct screen */if (typeof SubmitForm === "function"){SubmitForm();}}catch(e){alert("Bookmarklet error: " + e.message);}})();
